ALTER TABLE "FT_VENDAS" 
ADD COLUMN "VALOR_TOTAL" DECIMAL(10, 2);

UPDATE "FT_VENDAS"
SET VALOR_TOTAL = t1.QTD_VENDIDA * t2.PREÇO_UNITÁRIO
FROM "FT_VENDAS" AS t1
JOIN "DIM_PRODUTOS" AS t2
ON t1."ID_PRODUTO" = t2."ID_PRODUTO";
